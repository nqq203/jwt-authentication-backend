class User {
  constructor(username, fullName, email, password, avatar) {
    this.username = username;
    this.fullName = fullName
    this.email = email;
    this.password = password;
    this.avatar = avatar;
  }
}